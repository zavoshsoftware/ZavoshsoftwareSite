@model IEnumerable<Models.Page>

@{
ViewBag.Title = "فهرست صفحات";
    
    Layout = "~/Views/Shared/BackEndLayout/_BackEndLayout.cshtml";
}

<h2>@ViewBag.Title</h2>

        @(Html.Kendo().Grid(Model)
              .Name("Grid")
              .Columns(columns =>
              {
              columns.Bound(model => model.Order);
              columns.Bound(model => model.AverageRate);
              columns.Bound(model => model.Title);
              columns.Bound(model => model.PageGroup.Title);
              columns.Bound(model => model.UrlParameter);
              columns.Bound(model => model.ImageUrl).Template(@<text><img src="@item.ImageUrl" height="100px" /></text>);
                columns.Bound(model => model.IsActive);
                columns.Bound(model => model.CreationDate);
                columns.Bound(model => model.LastModificationDate);
               //   columns.Bound(model => model.Position.Title);

                columns.Bound(item => @item.Id).Title("دستورات").Filterable(false).Groupable(false)
                          .Template(@<text>
									@Html.ActionLink("ویرایش", "Edit", new {id = @item.Id}, new {@class = "k-button"})
									@Html.ActionLink("حذف", "Delete", new {id = @item.Id}, new {@class = "k-button"})
									</text>).Width(100);  
		})
		.ToolBar(toolbar =>
					{				
						toolbar.Custom().Action("Create","Pages").Text("افزودن");                          
					}
		)         
		.DataSource(dataSource => dataSource
									.Server()
							
									.Model(model => model.Id(item => item.Id))
					)     .Groupable()
              .Pageable()
             
                       
           
	)
